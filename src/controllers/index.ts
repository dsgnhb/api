import * as Donation from './donation/donation';
/*
const Chests = {
    add: Chests.add,
    remove: Chests.remove
};

const Coins = {
    add: Coins.add,
    remove: Coins.remove
};

const Levels = {
    findAll: Levels.findAll,
    fingByID: Levels.findbyID
};

const XP = {
    add: XP.add,
    remove: XP.remove
};

const Topdesign = {
    findAll: Topdesign.findAll,
    findByID: Topdesign.findbyID,
    findAllCurrentMonth: Topdesign.findAllCurrentMonth,
    findAllMonth: Topdesign.findAllMonth,
    add: Topdesign.add,
    remove: Topdesign.remove,
    changeStatus: Topdesign.changeStatus
};*/
export = Donation; /*
export = Chests;
export = Coins;
export = Levels;
export = XP;
export = Topdesign;*/


